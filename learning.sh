# Loki:

Promtail is use to collect logs from the server, and will stream to Loki. We will get the logs, which will have label as a metadata that will have a index.
We can use local filesystem to store the log, as well as to the cloud.
We will be using LogQL to get the data from Loki.